#pragma once

// i go in a door, we call this, and we load the lavel
//checks if the scene to load in is combat or field related, then chooses from a list of either combat encounters(if combat) or field screens(if field)
class SceneMgmt
{
};

